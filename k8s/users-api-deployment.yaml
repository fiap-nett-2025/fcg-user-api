apiVersion: apps/v1
kind: Deployment
metadata:
  name: fcg-users-api
  labels:
    app: fcg-users-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fcg-users-api
  template:
    metadata:
      labels:
        app: fcg-users-api
    spec:
      containers:
        - name: fcg-users-api
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/fcg-users-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: AWS_REGION
              value: "${AWS_REGION}"
            - name: AWS_DEFAULT_REGION
              value: "${AWS_REGION}"
          envFrom:
            - secretRef:
                name: aws-credentials
            - configMapRef:
                name: fcg-users-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
